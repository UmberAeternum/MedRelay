# Design decisions

MedRelay uses a focused dark interface with cyan safety/status accents. The primary layout remains usable at 360 px and separates patient input from the clinician-review handoff. Labels explicitly distinguish synthetic demo data, deterministic offline output, and a successful live GPT response.

Visual spectacle was intentionally reduced so warning guidance, information gaps, evidence, uncertainty, and editing controls remain clear.
