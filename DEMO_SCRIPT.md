# Demo script

Open `/medrelay` and point out “Demo — synthetic data only.” Choose a labelled Telugu, Hindi, English, or warning-sign scenario. Show that readiness states come from the validated server response. Create the clinician-review handoff, edit a field, expand Evidence and audit trail, and copy it. Add “Actually…” as a correction and regenerate to show the latest correction superseding the displayed symptom while the transcript evidence remains. Finish with Reset demo.

If no API key is configured, explicitly show the offline label. Do not describe a static deterministic result as GPT output.
